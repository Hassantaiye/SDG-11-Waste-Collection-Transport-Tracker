# Example environment variables for the server
# Do NOT commit real secrets. Copy to .env and fill with real values before running.

PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/mydb?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here
NODE_ENV=production
CLIENT_URL=http://localhost:3000
